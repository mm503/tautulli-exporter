apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tautulli-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "tautulli-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', '{{ include "tautulli-exporter.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
